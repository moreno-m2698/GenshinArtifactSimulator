import { describe, assert, it } from 'vitest'
// it is an alias for test in 
//timeout is time in milliseconds

describe("utilities scripts", () => {
    describe("helper.ts", () => {
        describe('repeatFunction', () => {
            it('repeats addition', () => {

            })
        })
    })

})